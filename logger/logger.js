const codename = "OrbitComms";

const log = (msg) => {
    console.log(`[${codename}] ${msg}`);
}

const warn = (msg) => {
    console.warn(`[${codename}] ${msg}`);
}

const error = (msg) => {
    console.error(`[${codename}] ${msg}`);
}